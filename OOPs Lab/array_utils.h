#ifndef ARRAY_UTILS_H
#define ARRAY_UTILS_H

#include <vector>

namespace ArrayUtils {
    double average(const std::vector<int>& arr); // Function to calculate average
    int maximum(const std::vector<int>& arr);   // Function to find maximum
}

#endif // ARRAY_UTILS_H
